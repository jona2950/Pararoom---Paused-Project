package perdiarom.shape.trigonometry.test;

import perdiarom.shape.trigonometry.SolutionPath;

public class trySolutionPath {

	public static void main(String... args) {
		SolutionPath test = new SolutionPath(1,1,0);
		test.test_unknowns();
		test.getHash();
	}
}
